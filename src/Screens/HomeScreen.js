import { Header, Info, Navbar, Sidebar } from '../Components';

const HomeScreen = () => {
    return (
        <div>
            <Navbar />
            <Sidebar />
            <Header />
            <Info />
        </div>
    );
};

export default HomeScreen;