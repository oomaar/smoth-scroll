import { Navbar, Sidebar } from '../Components';

const HomeScreen = () => {

    return (
        <div>
            <Navbar />
            <Sidebar />
        </div>
    );
};

export default HomeScreen;
