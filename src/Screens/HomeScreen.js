import { Header, Navbar, Sidebar } from '../Components';

const HomeScreen = () => {

    return (
        <div>
            <Navbar />
            <Sidebar />
            <Header />
        </div>
    );
};

export default HomeScreen;
